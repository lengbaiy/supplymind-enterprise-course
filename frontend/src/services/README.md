# Frontend services

The future API client, token refresh flow, SSE parser, error mapping, and file
download adapters belong here. Components must not construct ad-hoc fetch calls
once the extraction begins.
