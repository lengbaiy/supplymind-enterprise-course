export type ReportRow = { id: string; title: string; status: string; created_at: string };
export function ReportList({ reports, onOpen, onDownload, onPreview }: { reports: ReportRow[]; onOpen: (id: string) => void; onDownload: (id: string) => void; onPreview: (id: string) => void }) {
  return reports.length ? <section className="report-list list-scroll">{reports.map((report) => <article className="list-row" key={report.id}><button className="row-main-button" onClick={() => onOpen(report.id)}><strong>{report.title}</strong><p>{new Date(report.created_at).toLocaleString("zh-CN")} · {report.status}</p></button><div className="report-row-actions"><button className="secondary-button" onClick={() => onOpen(report.id)}>查看详情</button><button className="secondary-button" onClick={() => onPreview(report.id)}>预览 PDF</button><button className="secondary-button" onClick={() => onDownload(report.id)}>下载 PDF</button></div></article>)}</section> : <p className="detail-hint">还没有报告，请从分析会话发起问题。</p>;
}
