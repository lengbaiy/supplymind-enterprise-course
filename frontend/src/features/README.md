# Feature modules

Business screens are grouped by domain: identity, datasources, knowledge,
analysis, reports, dashboards, and audit. Each feature owns its views and local
state while shared API/SSE calls remain in `src/services`.
