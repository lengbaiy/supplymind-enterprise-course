# 试运行候选版验收记录

更新时间：2026-08-21。此记录只陈述本仓库在本地 Compose 环境的可复现检查；模型服务由内网批准的兼容端点提供时，仍须在目标试运行环境执行一次人工业务验收。

## 已完成的自动化检查

- Alembic 当前版本：`0024_task_lifecycle_fields (head)`。
- 容器内后端：`pytest -q` 通过 31 项；`ruff check app scripts tests` 通过。
- 前端：生产构建通过，Vitest 通过 15 项。
- 浏览器：容器化前端（`http://127.0.0.1:5173`）已通过登录、路由深链、移动导航、角色限制、数据源连接测试、知识库即时删除、PDF 新窗口预览、会话窗口和流式分析结论主路径。
- Compose：开发示例与 `trial` 覆盖配置均可解析；试运行覆盖以显式迁移服务运行 Alembic，API 启动不隐式迁移。

## 人工发布验收

在填妥未提交的 `.env.trial` 后，按 [TRIAL_DEPLOYMENT.md](TRIAL_DEPLOYMENT.md) 依次完成：备份、构建不可变标签、显式迁移、启动、`live`/`ready` 检查。随后分别用组织管理员、分析师、只读成员和第二组织账号验证成员管理、资源隔离、真实数据源分析、PDF 下载和 Worker 重启恢复。

## 观察到的恢复行为

摄取、Schema 同步、大屏刷新、PDF 导出和分析运行均保存开始/结束时间、尝试次数与错误摘要。看门狗会重新入队仍可安全重试的摄取任务；其余外部副作用任务转为失败并保留 Trace ID、失败原因及相应的重试入口，避免界面永久停留在“处理中”。
